*.qpf *.qdb *.vstf 
.venv
.vscode
*.ini
db
incremental_db
*.lnk
output_files
build/*


# Ignore everything
*

# But don’t ignore directories (so Git can recurse)
!*/

# But don’t ignore files *with* extensions
!*.*
